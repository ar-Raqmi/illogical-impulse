# ############ Devices #############

device {
    name = mice # run hyprctl devices to get the name
    sensitivity = -0.2 # -1.0 to 1.0, 0 means no modification.
    accel_profile = flat # flat, adaptive, or nothing
    
    # Other useful options
    # scroll_factor = 1.0
    # natural_scroll = false
    # left_handed = false
    # middle_button_emulation = false
}
